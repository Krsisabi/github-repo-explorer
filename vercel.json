{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run prepare:types && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/|@|src/|assets/|node_modules/).*)", "destination": "/index.html" }
  ]
}
